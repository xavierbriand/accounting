# Story maint-27 retrospective

**PR:** [#227](https://github.com/xavierbriand/accounting/pull/227)  **Closed:** pending merge  **Closes issue:** [#223](https://github.com/xavierbriand/accounting/issues/223)  **Supersedes:** [Dependabot PR #221](https://github.com/xavierbriand/accounting/pull/221)

R15 major-bump-zero-code-shape story bundling a CI Node runtime upgrade (20 → 24 LTS), a new `package.json` `engines` floor (`>=22.12.0`), and landing `commander` 14.0.3 → 15.0.0. Distinctive among this repo's R15 precedents in that its *entire shape was pre-scoped by a different, concurrent session* — [issue #223](https://github.com/xavierbriand/accounting/issues/223) was filed the day before this story started, by a session running story-4.5's pre-planning maintenance sub-loop, which correctly diagnosed that `commander` 15 requires Node ≥22.12 while CI was still pinned to Node 20, and proposed exactly the story shape executed here. Diff: 1 line in `.github/workflows/ci.yml`, 4 lines in [package.json](../../package.json) (`engines` + `commander`), ~11 lines in `package-lock.json`, 2 lines in [CLAUDE.md](../../CLAUDE.md) / [docs/architecture.md](../../docs/architecture.md), 0 LOC in [src/](../../src/) or [tests/](../../tests/). Ran in the same session as its sibling [story-maint-28](story-maint-28.md) (dev-deps group bump).

## Keep

- **Trusting and executing a well-scoped issue from a different session, rather than re-deriving the analysis from scratch, was the right call.** Issue #223's technical claims (`commander@15.0.0` engines requirement, `better-sqlite3` 12.10+ dropping Node 20 prebuilds, the Node LTS schedule dates) were independently re-verified twice in this session — once during initial planning, once by the Phase 2 `sibling-overlap` agent — and held up exactly as stated both times, live against `npm view` and the official `nodejs/Release` schedule. Re-deriving from zero would have been pure duplicated effort; re-*verifying* a prior session's claims before building on them caught nothing wrong but was the correct level of diligence.
- **Picking the Active LTS (Node 24) over the Maintenance LTS (Node 22) with an explicit, checked rationale**, rather than defaulting to "the lower number is safer." Both satisfy the `>=22.12.0` floor, but Node 22 entered Maintenance LTS 2025-10-21 (EOL 2027-04-30) while Node 24 is Active LTS through 2026-10-20 (EOL 2028-04-30) — checked against `nodejs/Release`'s `schedule.json` directly rather than assumed. Longest-runway choice, stated as an explicit rejected-option in § "Selected solution" rather than left implicit.
- **The composition-root subprocess test (`tests/integration/cli/status-program.test.ts`) already existed and needed no new test**, correctly identified via R4's trigger condition (it fires when `program.ts` itself is touched — it wasn't here) rather than mechanically adding a redundant test "just in case." Phase 4 review independently confirmed the existing test is genuinely subprocess-tier (`spawnSync` against the compiled binary, not an in-process call) and re-ran green against commander 15 as part of this story's own probe.

## Change

- **A. Two Phase-2/DoR-timeframe docs commits (a stale cross-reference fix, and recording the Phase 2 review findings) landed as separate story-id-bearing commits instead of folding into the prep commit, inflating the R15 envelope from the declared 3 slices to 5 — caught by Phase 4 review, not by this session's own drafting.** R30's text is explicit ("DoR/PR-link edits fold into the prep commit, Phase-4/5 doc updates into the retro commit"), but in the moment, each correction felt like "just add a small commit" rather than "amend the still-open prep-commit-equivalent." Fixed by a `git reset --soft` back to the merge-base and recommitting with both corrections folded into the prep commit — same mechanism [story-maint-28](story-maint-28.md) needed for its own id-collision fix, run concurrently in the same session. **Second data point this session** that R30's fold-vs-new-commit distinction needs to be checked actively while drafting, not assumed to be obvious in the moment.
- **B. A real, distinct constraint change (the Node-version floor) didn't automatically propagate to the two canon docs that state it in prose (`CLAUDE.md` § 1, `docs/architecture.md` § "Technical dependencies"), even though the actual enforcement mechanism (`package.json` `engines`, `.github/workflows/ci.yml`) was correct from the first draft.** DoD item 10 ("New rules/constraints land in the same PR as a CLAUDE.md / `docs/` edit") is written with *behavioral/process* rules in mind (the § 8 rule-provenance table), and this session's drafting didn't extend that same instinct to a plain factual constraint like a runtime-version floor stated in prose elsewhere. Phase 4 review caught it by reading both files directly, not by any automated check — `drift-scan`'s structural scope (plan↔source, § 8 rule-tag consistency) doesn't cover free-prose version-number staleness, and no other CI step does either (see § Try).
- **C. This story is the 3rd consecutive R15/R26 story to reproduce the identical "no § 6 lane trigger literally covers this diff's actual surface" gap**, now unambiguously past whatever threshold this repo's own convention uses to decide "codify, don't just note again" — except the fix (adding R15 to § 6's Envelope column, or reconciling the trigger wording) is already fully scoped in the still-open [issue #200](https://github.com/xavierbriand/accounting/issues/200), filed after the 2nd occurrence (story-maint-22). A 3rd occurrence without #200 being picked up is a signal about backlog prioritization, not about needing a 4th retro to restate the same finding.

## Try

- **File a harness follow-up for Change B: no automated check catches free-prose version/constraint staleness in `CLAUDE.md`/`docs/architecture.md` when the underlying enforcement mechanism (a config file, a CI pin) changes.** `drift-scan` today only checks structural plan↔source and § 8 rule-tag consistency — a "the Node version mentioned in CLAUDE.md doesn't match `package.json` engines" check would be a new, narrow rule, possibly not worth generalizing beyond this one recurring fact (Node version) unless it recurs with other constraints too. Filed as [issue #229](https://github.com/xavierbriand/accounting/issues/229) covers a related but distinct gap (no CI `npm audit` step) — worth considering whether a canon-doc-staleness check belongs in the same follow-up or a separate one, if this pattern reproduces on a future runtime/toolchain bump.
- **Re-prioritize [issue #200](https://github.com/xavierbriand/accounting/issues/200) now that it has a 3rd occurrence (this story) beyond the 2 that already justified filing it.** Sitting open across 3 R15-shaped stories without action is itself a data point that "file an issue and wait" isn't sufficient signal for this particular backlog item — worth an explicit next-maintenance-sub-loop check on whether to actually schedule it.
- **Watch whether the R30 fold-into-prep-commit habit (Change A) sticks now that two sibling stories in the same session both needed the same rewrite.** If a future maintenance story reproduces this a third time, it's worth an explicit checklist line in [docs/templates/maintenance-sub-loop.md](../templates/maintenance-sub-loop.md) rather than relying on Phase 4 review to keep re-catching it.

## Action items

| Item | Where it lands | Status |
| --- | --- | --- |
| A. Consider whether canon-doc version/constraint staleness (Change B) deserves its own drift-scan-style check, alongside the already-filed npm-audit-CI-step gap. | [Issue #229](https://github.com/xavierbriand/accounting/issues/229) covers the audit-step half; canon-doc staleness not yet separately filed — revisit if it recurs. | open, passive |
| B. Re-prioritize issue #200 (R15/lane-table reconciliation) given this is its 3rd occurrence. | Next maintenance sub-loop. | open |
| C. Watch for the R30 fold-into-prep-commit gap recurring a third time (Change A / Try); promote to an explicit template line if it does. | Retro of the next maintenance story. | open, passive |

## Loop metrics (this run)

- **Plan phase:** 1 maintenance sub-loop (shared with sibling story-maint-28) + technical re-verification of issue #223's claims (commander engines requirement, better-sqlite3 prebuild drop, Node LTS schedule — all confirmed live) + Phase 2 `sibling-overlap` review (4 findings, all resolved/acknowledged).
- **Implementation:** Phase 3 collapsed into the Phase 1 probe (R15 precedent). No Sonnet invocation.
- **Phase 4 review:** first attempt stalled/failed after 600s with no progress (background-agent watchdog timeout, no findings produced) and was retried once, successfully. Second attempt: `code-reviewer` (4 findings, all P3: 2 fix-now — envelope-inflating extra commits, stale canon-doc Node-version references; 1 defer-issue — no CI `npm audit` step, filed as #229; 1 acknowledge — recurring #200 gap; plus 2 soft acknowledges). All fix-now items resolved via a `git reset --soft` + force-push cycle.
- **CI round-trips:** 2 on this PR (1st: green, before the Phase 4 fixes; 2nd: green again, after the commit-history rewrite folding in the fixes).
- **Issues closed by this story:** [#223](https://github.com/xavierbriand/accounting/issues/223) (via merge). [#221](https://github.com/xavierbriand/accounting/pull/221) closed as superseded.
- **Issues opened:** 1 — [#229](https://github.com/xavierbriand/accounting/issues/229) (npm-audit CI-step gap, Phase 4 defer-issue).
- **Total commits on branch:** 3 (prep / ci-bump / empty-refactor) + this retro = 4, matching R15's nominal target exactly on the final, corrected history.
- **Test count:** 1010 product + 1 skipped + 252 harness → unchanged throughout every probe and CI run, including on the new Node 24 CI runner.
- **Diff stats (final):** 1 line `.github/workflows/ci.yml` + 4 lines `package.json` + ~11 lines `package-lock.json` + 2 lines across `CLAUDE.md`/`docs/architecture.md` + ~142 LOC plan + 0 LOC `src/`/`tests/`/`harness/`.
- **Bugs squashed:** 0 (dependency-hygiene story). **Process bugs surfaced:** 2 (R30 fold-vs-new-commit gap; canon-doc version-staleness gap with no automated check) + 1 already-tracked recurrence (#200, 3rd occurrence).
- **`npm audit --audit-level=high`:** 0 → 0 findings throughout (checked locally every probe; no CI step exists to check it automatically — see § Try / issue #229).
- **New runtime deps:** 0 (major-version upgrade of an existing dependency). **New dev deps:** 0.
- **Time-to-DoD:** issue re-verification + probe + plan ~20 min; Phase 2 review ~4.5 min agent time; 1st CI round (green) ~3 min; Phase 4 review 1st attempt (stalled, ~10 min wasted) + retry (~8 min agent time) + resolving findings via history rewrite ~15 min; 2nd CI round (green) ~3 min; this retro ~10 min.

## Carryovers resolved

- **[#223](https://github.com/xavierbriand/accounting/issues/223) (CI Node runtime upgrade + commander 15 major bump, filed by a prior/concurrent session)** → **CLOSED by this story** (via PR #227 merge), executing exactly the shape it proposed.
- **[#221](https://github.com/xavierbriand/accounting/pull/221) (Dependabot's own commander bump PR)** → **superseded and closed**, no independent code change on that branch beyond the version bump itself.
- **better-sqlite3 Node-20-prebuild fragility (noted in issue #223, from PR #219 already merged to `main`)** → resolved as a side effect of this story's Node-floor upgrade; not independently re-verified as a standalone fix, but no longer a live concern once CI runs on Node 24.
