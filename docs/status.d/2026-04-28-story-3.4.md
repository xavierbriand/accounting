---
date: 2026-04-28
story: 3.4
epic: 3
---

Story 3.4 shipping. Safe Monthly Transfer Calculator: `SafeTransferCalculator` (pure Core, composes `SplitRulesService` + `BufferStateService` + `RecurringForecastService`) returning aggregate-per-partner + flat line-items. Per-occurrence split application (PRD "Job Loss" semantics). Date-driven buffer top-ups via new `targetDate` field on `BufferBucket`: `(target − balance) ÷ monthsBetween(asOf, targetDate)` with month-start anchored line items, LRM-distributed across the fill schedule. Two failure modes: stale targetDate with shortfall, monthsRemaining=0 with shortfall — both surface a "set a new targetDate" actionable error. Phase 4 caught a CRITICAL uncommitted-interface bug (`BufferState.targetDate` declared in working tree but not in HEAD) + two property-test honesty issues + one floating-point trap I introduced while strengthening Property #1.
