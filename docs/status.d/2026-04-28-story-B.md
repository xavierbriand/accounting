---
date: 2026-04-28
story: B
pr: 82
---

Story B shipping (#82, closes #74). YAML becomes the only source of `autoTagRules`: grouped-by-category schema, regex pre-compiled in superRefine, category strings validated via Story A's `validateNewCategoryName` (relocated to `src/core/categories/`). `DEFAULT_RULES` deleted; `TransactionBuilder` rules param required; `program.ts:104` wires `config.autoTagRules`. `accounting.example.yaml` carries the 8-category migration; user's local `accounting.yaml` updated via manual diff in the PR description.
