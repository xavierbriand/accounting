---
date: 2026-04-28
story: maint-16
pr: 85
epic: M-A
---

story-maint-16 merged (#85). Parallel-safe dev loop: fragments the `docs/status.md` status log into per-story `docs/status.d/<date>-story-<id>.md` files (27 history entries + 1 pre-history sentinel migrated; `docs/status.md` retains only Current position + Refresh trigger + pointer). Adds worktree push protocol to CLAUDE.md § 6.4.1 (one agent per branch, never push `main`, fetch+rebase+conflict-diagnosis before push). Updates maintenance sub-loop checklist to include sibling-work check and shifts `git fetch` to push time. Codifies R16 (R15-extension to any zero-behaviour-change story), R17 (status.d fragment convention), R18 (push protocol), R19 (sibling-work check). First fragment authored under the new convention.
