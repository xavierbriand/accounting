---
date: 2026-04-29
story: C
pr: 84
---

Story C shipping (#84, closes #75). 'Remember this rule' prompt + atomic YAML write-back: pure `suggestPattern` Core helper (longest alphabetic ≥4-char token, noise-filtered) + `ConfigWriter` port + `YamlConfigWriter` infra (`parseDocument` round-trip, mtime race guard, conflict reject, atomic tmp+rename, sanitizeFsError); `confirmRememberRule` y/e/n prompt with compile-and-match validation + 200-char ReDoS cap; YAML-write-before-DB-commit ordering with new exit code 5; `--scripted-prompts` test-only flag for R4 subprocess driving. Closes the inline-categories trio. Surfaced + fixed a latent Story-A bug (change action wasn't propagating to `transaction.entries[].account`). BDD round-trip scenario closes Story A retro Try-list carry-over. Three-data-point security-checklist user-label carve-out codified.
