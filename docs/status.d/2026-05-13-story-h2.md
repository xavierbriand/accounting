story-h2 shipped (#96). Module 2 of the harness-engineering curriculum. Added one Claude Code primitive of each type: slash command (`/story-status`), skill (new-story-preflight), Stop hook (lint check), statusline (branch·lint·story), sub-agent spec (sibling-overlap), MCP server (GitHub read-only). First story to use parallel Phase 2 sub-agents.
