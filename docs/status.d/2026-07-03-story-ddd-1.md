# 2026-07-03 — story-ddd-1: DDD adoption (model first, then forward)

Domain-Driven Design practice adopted, zero production-code change. The latent model is now explicit and user-owned: ubiquitous-language glossary + context map at `docs/domain/` (single bounded context "Shared Finances"; Epic-5 split tripwire recorded), per-story model notes, `ddd-modeler` agent (Mode A candidate shapes / Mode B conformance scan) + `model-session` skill, Phase 0 "Model" and a Phase-4 conformance leg wired into CLAUDE.md § 6.1 (R24/R25). Architecture decisions added: named DDD patterns; domain events as plain value objects via a port, entering with Epic 4. `docs/templates/plan-template.md` created (dangling preflight reference resolved). "Next" unchanged: Epic 4 — whose story definition now starts with the first real modeling session.
