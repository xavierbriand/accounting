---
date: 2026-07-04
story: h10a
pr: 171
epic: harness-engineering
---

Single-source the rule system — **specs half** of #162 (F5 + F9), split from the drift-scan
guard **h10b** at planning. Both reviewers' rule-coverage walks were frozen at a hard-coded
`R1..R15` enumeration, hiding every later rule from the agents chartered to enforce them.
Converted both walks to a **row-driven** walk over the live § 8 table (anchored
`grep -nE '^\| R[0-9]+ \|'`, yielding exactly 25 rows — the § 8 numbering hole between R21 and
R23 is handled by iterating rows, never a range); counters became `M / <§ 8 row count>`.
De-duplicated: removed the phantom "Missing scenarios…" quote falsely attributed to CLAUDE.md,
unified R9's carve-out to § 8's three criteria, un-trapped sonnet-implementer's four case-law
blocks from its return-format code fence (new § 4a) and fixed its stale § 7 pointer. Reconciled
`CLAUDE.md` §6.2 (scan/execute Sonnet vs judge/decide Opus, all agents listed against their
frontmatter tier) and §6.3 (all seven return sections, actual titles). Hardened `sibling-overlap`
(pinned `model: sonnet`, `--limit 50`, positive-case output schema) and reconciled
new-story-preflight's worktree convention with actual session-branch practice (R18). Ride-along:
`harness-engineering.md` § 8 references + three Part-A "closed by story-hX" notes. Reduced lane,
Opus-authored (no Sonnet round); Phase-4 code-reviewer caught the unanchored-grep over-count in
the very mechanism the story ships (fixed). **Next: story-h10b (#162)** — extend drift-scan over
`.claude/` to guard these specs.
