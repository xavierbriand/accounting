---
date: 2026-07-04
story: h10b
pr: 173
epic: harness-engineering
---

Drift-scan guard — **scanner half** of #162 (F5, scope item 3), paired with **h10a** (specs half,
PR #171). Adds **Check D — `.claude/` rule-tag drift**: always scans the full
`.claude/agents/*.md` + `.claude/commands/*.md` corpus (not diff-scoped, like Check A) for an
enumerated rule-range antipattern (`R1..R15`, dash/ellipsis variants — always flagged, no
legitimate use) and bare `R<n>` references not in the live § 8 set (`*(hole)*` marker suppresses a
deliberate reference, mirroring Check A's `*(pending)*`). New `drift-parser.ts` exports:
`extractEnumeratedRuleRanges`, `extractClaudeTagRefs`, `composeClaudeDrift`; new `drift-scan.ts`
`runClaudeCheck` wired into `main()`'s exit gate and `formatHumanReport`. Marks both h10a-authored
"§ 8 skips R22 (no tombstone row)" mentions `*(hole)*` so the clean-repo scan passes without
resolving the numbering hole (stays #164's job); extends the PostToolUse hook to fire drift-scan
on `.claude/(agents|commands)/*.md` edits; widens R21's § 8 wording (no new R-tag minted). Reduced
lane, real Sonnet TDD round (restores the two-model split after h10a's Opus-only run). 47
drift-scan tests (up from ~30); full suite green. **Next: story-h10 pair (#162) closes** — no
committed successor yet.
