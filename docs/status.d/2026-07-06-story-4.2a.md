---
date: 2026-07-06
story: 4.2a
pr: 184
epic: 4
---

Story 4.2 (Correction) split into 4.2a (domain + persistence) and 4.2b (CLI + recording); 4.2a shipped. The **reverse-and-correct** domain: `Transaction` gained `kind` ∈ {`original`,`reversal`,`correcting`} + `correctsId`; a pure `CorrectionService.correct(original, changes, ids, reason)` returns `{ reversal, correcting, event }` — the reversal mirrors the original (nets it to zero in its own period), the correcting entry posts the truth, the original is never mutated; `TransactionCorrected` joined the `DomainEvent` union (returned, not yet recorded). Migration 006 rebuilt `transactions` (SQLite 12-step, migration-004 idiom) to add `corrects_id`/`kind` and relax `idempotency_hash` to nullable under a **kind-conditioned CHECK** (`original` ⇒ hash present; correction rows ⇒ hash NULL) — turning the story-4.0 ingest-ACL firewall into a DB invariant. `saveCorrection` persists the pair atomically, hash-free. Scoped to the two-entry expense shape; >2-entry split-correction deferred to #183. Date rule (user-approved): reversal always keeps the original date, the correcting entry takes the new date only when `date` is corrected — a proposed delta to the 4.0 note's Invariant 6, pending reconciliation. Audit-event atomicity stays B1 (recorded at the boundary in 4.2b; #180 remains the UnitOfWork story). `ddd-modeler` Mode B: 0 conformance violations. Next: **story-4.2b** — the `correct` CLI (explicit flags) + boundary recording of `TransactionCorrected`.
