---
date: 2026-07-08
story: 4.3a
pr: 204
epic: 4
---

Story 4.3 (FR19) **reframed by user interview** before planning: not correction narration but a **settlement variance report** — why this month's suggested transfer differs from last month's, itemized penny-perfectly per cause and per partner, plus a follow-through check of last month's actual transfers (ledger credits on config-named settlement accounts) against the suggestion. Corrections fold into balances silently; the per-transaction correction view is deferred with its feasibility-verified design ([#202](https://github.com/xavierbriand/accounting/issues/202)). 4.3a ships the Core+Infra half: `src/core/settlement/` (`LineItemKey` exact-match cause identity, `VarianceLine` movement with signed Money deltas, `FollowThrough` per-partner, `SettlementVariance` report, `explainSettlementVariance` pure domain function), `ContributionQuery` port + `SqliteContributionQuery` (net credits−debits so corrections net out), and the `settlement:` config section (partner roster-checked). Movement diffs two `SafeTransferCalculator` runs with as-of dates one month apart — no new calculation machinery, no migration, no events; 10 signed invariants all property/unit-tested (`ddd-modeler` Mode B: enforced). Phase 4 dropped the modeled totals-only fallback as unreachable from documented config (user decision; model-note refinement + glossary updated). New rule **R30** (envelope-exempt docs-commit subjects) from a dod-check near-miss. Epics story text rewritten; 4.3 no longer depends on 4.2. Next: **story-4.3b** (CLI `explain` command, [#208](https://github.com/xavierbriand/accounting/issues/208) intake).
