---
date: 2026-07-08
story: 4.3b
pr: 210
epic: 4
---

**FR19 ships in full** — story-4.3b lands the month-scoped `explain` command over 4.3a's settlement-variance domain: `runExplainCommand` (status-pattern orchestrator, exit codes 0/1/2, injected clock), side-by-side variance table + follow-through + blameless CFO headline (human) and a documented `--json` shape (`Money.toString()`, Maps→objects). Independently-tolerant sections: a failed calculation or missing `settlement:` config degrades only its own section with a Suggested action (exit 0). New shared `src/cli/utils/settle-window.ts` owns the window/as-of composition (as-of dates one month apart; the silent-zero trap documented at the single source) — consumed by explain, status, and the 4.3a acceptance steps. #208 intake: shipped items 1 (helper), 2 (determinism property widened to the full report), 4 (R8-diverse JSON fixtures); item 3 (adapter netting-loop extraction) stays honestly deferred. R4 subprocess journey asserts a genuine read-only guarantee (no snapshot **and** no rows written). Phase 4: 15+2 findings, 5 fix-now applied (16 R6 clauses, R4 strengthening, single-domain-call refactor, shared `report-format`/`report-command` utils); envelope exactly 10/10 (R28). Implementation ran via the § 6.3 gp+inline-spec fallback (Sonnet-pinned) after the registered `sonnet-implementer` type failed at launch — signature logged to #166. Next: **story-4.4** (global `--json` audit, FR20) / **4.5** (config-change + dissolution events; unblocks Epic-5 5.4).
