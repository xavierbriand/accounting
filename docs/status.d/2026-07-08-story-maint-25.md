---
date: 2026-07-08
story: maint-25
pr: 212
epic: maintenance
---

story-maint-25 (pending merge): triages all 155 `local/conditional-test-logic` ESLint hits then-current on `main` (follow-up from [story-maint-24](story-maint-24.md), closes [issue #206](https://github.com/xavierbriand/accounting/issues/206)) via a 26-agent workflow that classified every hit, then a synthesis pass cross-checking every proposed rule exclusion against the full dataset for counter-examples. Adds 3 new rule-exclusion patterns (fast-check precondition/filter skip, top-level Result/nullable narrowing guard, `finally`-block cleanup guard — 155 → 105 hits) and applies 55 mechanical genuine-smell fixes across 18 test files (105 → 96 hits), fanned out via an 18-agent Workflow with per-file test verification. Found and fixed a bonus bug along the way: `canonicalize.test.ts`'s `'direction'` tampering branch was setting `description` instead, never actually exercising that field. 15 bigger structural refactors filed as [issue #211](https://github.com/xavierbriand/accounting/issues/211); 13 hits acknowledged as genuinely unavoidable. `npm test`: 928 passing + 1 correctly-skipped, 0 failures.
