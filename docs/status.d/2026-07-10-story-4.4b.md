---
date: 2026-07-10
story: 4.4b
pr: 216
epic: 4
---

Global JSON contract shipped — FR20 complete (story-4.4 shipped in full: 4.4a behaviour
+ 4.4b contract). All five `--json` commands emit one compact envelope: `{command,
ok: true, data}` on stdout; `{command, ok: false, error: {code, message,
suggestedAction?, details?}}` as the final stderr line on failures, with an 8-code
`JsonErrorCode` registry disambiguating the overloaded exit 2 (exit codes themselves
unchanged). Conventions normalized per the LLM-agents-as-consumer reframe: camelCase
keys, `Money.toString()` amounts, compact printing; ingest's needs-review payload moved
to `error.details`; categorize's two silent paths fixed; correct's `changedFields`
realigned to domain `account`. Contract documented at docs/cli-json-contract.md
(streams discipline: branch on exit code before trusting stdout) — kept current by new
rule R31 (same-PR contract-doc updates for any `--json` shape change). `migrate`
excluded by decision. Deferred: audit finding 7 → #180, `rulesSkippedAsDuplicate`
reintroduction → #104, empty-batch caveat → #215. Unblocks #186 (e2e journeys) and
#213 (`--dry-run`).
