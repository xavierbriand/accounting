---
date: 2026-07-17
story: 4.5a
pr: 230
epic: 4
---

Config-change detection shipped — the first half of the 4.5 split (FR23 completion for config
changes). Every ledger-opening command now ambiently diffs `accounting.yaml` against the
last-seen state (`config_state`, migration 007) through a pure `ConfigChangeDetector` and records
`ConfigChanged` (origin `'external'`; Epic-5 5.4 fills `'applied'`) with a verbatim,
identity-keyed old→new diff — magnitudes included, which is what #203's `explain` labelling
needs. Cosmetic YAML edits never emit (canonical form + `HashFn` digest; `Money` via
`Money.toString()` so dinero bumps can't shift it; `dbPath` excluded). PII risk is managed at the
source: the glossary's new **Configuration (accounting.yaml)** invariant (PII-safe by
construction) is enforced by a recursive parse-time tripwire (mod-97 IBAN / Luhn card,
path-cited). `categorize` keeps its story-D no-DB invariant; story-3.5's read-only `status` AC
gained a deliberate, user-approved audit-observation exception. Glossary vocabulary for both 4.5
halves landed under user sign-off (Dissolution promoted; Export bundle, Dissolution receipt,
Config change, Configuration added; `DataExported` minted at Phase 0). Next: story-4.5b
(dissolution — export bundle + proof-gated wipe + receipt) completes FR21 and Epic 4 from the
same model note. Drift issues filed en route: #228 (story-D ESLint claim), #231 (config-load
exit-code mapping).
