---
date: 2026-07-17
story: 4.5b
pr: 233
epic: 4
---

The export act of dissolution shipped — `accounting export [--out] [--json]` writes ledger +
audit trail + config copy into an atomically-staged directory bundle (0700/0600, `.partial` +
rename, never overwrites), records `DataExported` *before* the write so the bundle's own
`domain-events.json` contains it (invariant 8), and prints the manifest-hash **export-proof**
that 4.5c's wipe will demand. Fidelity (invariant 9) round-trips through the project's own
`csv-parse`, including NULL-hash correction rows and hostile description fixtures. Hand-rolled
RFC-4180 escaper (no new dependency); `sanitizeFsError` generalized into a shared Infra helper;
contract doc gained the export envelope, widened `WRITE_FAILURE` mapping, and § Export bundle
format (config_state exclusion documented). Two signed model-note amendments at planning:
`DataExported` dropped `manifestHash` (circular with invariant 8 — proof is an artifact, not a
trail fact) and `archiveLocation` narrowed to the bundle basename (no paths in the append-only
trail). The 4.5b/4.5c split (two composed acts, two stories) plus CLI surface decisions
(`export`/`dissolve` verbs, directory bundle, typed-phrase + `--confirm` wipe UX) were
user-approved at planning. #155 closed (FR23 umbrella fulfilled); #232 filed
(restore-from-bundle). Next: **story-4.5c** — the proof-gated wipe + dissolution receipt,
completing FR21 and Epic 4.
