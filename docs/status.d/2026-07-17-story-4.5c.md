---
date: 2026-07-17
story: 4.5c
pr: 234
epic: 4
---

The proof-gated wipe shipped — **FR21 complete, Epic 4 complete**. `accounting dissolve
--bundle <path> [--confirm] [--json]` erases the ledger stores only past four gates: byte-based
bundle re-verification (shared manifest module — one hashing implementation with `export`),
**strict staleness** (append-only stores make count equality tail equality; dissolve runs the
standard ambient observation, so a config-edit-since-export is a *detected* staleness cause —
the loop's Phase-2 review reversed the draft's observation-skip and made the model stronger),
and typed-DISSOLVE / `--confirm` confirmation. The `dissolution-receipt.json` (0600,
fsync-durable, full archive path — a local file, not the trail) is written before any deletion;
the wipe runs aux-files-first-DB-last after `closeDb()`; `accounting.yaml` survives
byte-identical. `DissolutionPerformed` is **excluded from the `DomainEvent` union** — recording
it to the trail is a compile error, matching the signed note's receipt-only ruling. Mode B's
"silent coupling" catch became the story's key new test (config edit after export → refusal,
DB intact). Epic 4's event family is closed: ingested, corrected, config-changed, exported,
dissolved. Next: user's pick — Epic 5 (fully unblocked), the post-Epic-4 harness batch
(#164/#165/#166), or the overdue product bugs (#93/#103).
