---
date: 2026-07-17
story: maint-26
pr: 224
epic: maintenance
---

story-maint-26 (pending merge): closes a completeness gap in FR20/story-4.4b — Commander's own parse-time failures (missing required option/argument, unknown option, excess arguments) were caught by Commander's parser before any command's action handler ran, entirely bypassing the `--json` failure envelope (plain prose to stderr, exit 1, unconditionally). Surfaced from a user bug report (`npm run ingest --json` with `-f` omitted); root cause was two layered issues — an npm flag-forwarding footgun (no code fix, user-educated) plus this genuine gap, already flagged as a known omission in story-4.4b's `correct.feature` comment. Fix: `program.exitOverride()` + a catch around `program.parse()` in `src/cli/program.ts`, translating the known bad-usage `CommanderError` codes into the existing `INVALID_ARGUMENT` envelope, scoped to the 5 `--json`-capable commands, exit 2 unconditionally. Phase 4 `code-reviewer` caught a real scope-leak (the exit-2 change was initially unscoped and would have silently changed `migrate`'s exit code too) — fixed via a normal TDD round before merge. PR #227 (Node 20→24 LTS + commander 14→15 major bump) merged mid-story; re-verified empirically against commander 15.0.0, no changes needed. New rule tag: R32 (plan-mode verification after a transport-errored `ExitPlanMode` call).
