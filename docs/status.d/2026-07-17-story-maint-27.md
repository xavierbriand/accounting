---
date: 2026-07-17
story: maint-27
pr: 227
epic: maintenance
---

story-maint-27 (pending merge): CI Node runtime upgrade (`.github/workflows/ci.yml` node-version 20 → 24 LTS), a new `package.json` `engines` floor (`>=22.12.0`), and landing Dependabot [PR #221](https://github.com/xavierbriand/accounting/pull/221) (`commander` 14.0.3 → 15.0.0). Executes the shape proposed by [issue #223](https://github.com/xavierbriand/accounting/issues/223), filed by a different, concurrent session's story-4.5 pre-planning maintenance sub-loop before this session started — Node 20 is past EOL (2026-04-30, per the official `nodejs/Release` schedule) and `commander` 15 requires Node ≥22.12, both independently re-verified live. R15 major-bump-zero-code maintenance story, run in the same session as sibling [story-maint-28](2026-07-17-story-maint-28.md). Breaking-change audit against `src/cli/program.ts` (the sole in-repo `commander` importer) found zero code-change impact, confirmed by the full 1010-test product suite (including the composition-root subprocess test) + 252-test harness suite, both green — including on the new Node 24 CI runner. Also updates `CLAUDE.md` § 1 and `docs/architecture.md`'s runtime-version references from "Node.js 20" to the new floor, per DoD item 10. Phase 4 review caught two envelope-inflating docs commits (folded into the prep commit via a history rewrite) and filed [issue #229](https://github.com/xavierbriand/accounting/issues/229) (no CI `npm audit` step exists to independently verify the security checklist's supply-chain claim). Filed and closed issue #223; supersedes and closes Dependabot's own PR #221.
