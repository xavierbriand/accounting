---
date: 2026-07-18
story: E
pr: 236
epic: 2-followups
---

The overdue ingest bug shipped (#93 Option B, closes #103 — and #93 itself, Option A having
shipped as story D's `categorize`). A rule remembered mid-`ingest` now auto-tags every later
matching row of the same run: visit-time matching in `runInteractiveLoop` against the rules
remembered so far, the manual-change rewrite extracted into a shared `applyCategoryChange`
helper (auto and manual paths structurally cannot drift), `new RegExp(pattern, 'i')` identical
to the next-invocation config-load construction, forward-only + first-rule-wins pinned by
units, and a Phase-4 crash guard for syntactically invalid user-edited patterns (reachable via
`ScriptedPrompter`, which — unlike the inquirer prompter — passes patterns through unvalidated).
CodeQL's `js/regex-injection` on the construction is assessed as the feature itself
(user-authored regex, config-load parity); dismissal decision at the merge gate. Single
production file, zero implementer deviations, 4 slices.
