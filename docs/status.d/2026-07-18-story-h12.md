---
date: 2026-07-18
story: h12
pr: 235
epic: harness
---

Evals-lite shipped (#165, health-check F6, post-Epic-4 batch 1 of 3). The loop now measures its
own review economy: `harness/disposition-report` (`npm run metrics:dispositions`) aggregates 688
suggestion-log rows across 55 plans — three real table dialects, two discovered by chasing the
honest `unparsed` bucket against the actual corpus — into committed
`docs/metrics/dispositions.{md,json}` with per-rule acknowledge-only rates. The same PR acted on
the data: R1/R9/R10/R11 (81.8–100% acknowledge-only at n≥5) demoted to coverage-table-only in
the reviewer specs, while the #165-named suspects R6/R8/R12 were **refuted** (25–62.5%) and kept.
All six agent specs carry drift-scan-enforced `spec-version` headers (Check F
`missing-spec-version`; blank-value coercion fail-safe fixed at Phase 4). The
property-test vacuity check re-landed in sonnet-implementer, and #217's push-ownership hard
rule shipped — the spec itself had *instructed* pushing ("branch pushed" in mission + stop
conditions), so the 4.4b violation was obedience; both lines rewritten. Retro lesson minted:
cite the artifact, not the messenger (slice-6 prose used pre-final-run numbers; the reviewer
caught the drift against the committed artifact). Next: **story-h13** (subtraction, #164) —
its evidence base is now on the branch.
