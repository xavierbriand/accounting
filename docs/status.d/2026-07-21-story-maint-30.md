---
date: 2026-07-21
story: maint-30
pr: 250
epic: maintenance
---

The PRD is a live document again. Finding #3 of the 2026-07-20 critical review had it
promising five MVP verbs (`ingest`, `status`, `settle`, `config`, `correct`) against a
shipped surface of eight (`migrate`, `ingest`, `correct`, `status`, `explain`, `export`,
`dissolve`, `categorize`) — a canon doc Epic 5 planning will read. Reconciled by
annotation, not rewrite. Two user decisions taken at planning with the code evidence in
hand: **`settle` is annotated as absorbed**, not shipped as an alias — `status` computes
the safe transfer (FR8) and `explain` reports settlement variance + follow-through
(FR19), so settlement math happens on read and the couple executes the transfer in their
banking app; and the **`update --check` network sanction is struck** rather than kept as
post-MVP, making zero-outgoing-network unconditional. `config` is marked Epic 5
(FR24–27), and the five unlisted shipped commands enter with their origins. Phase 4
earned its keep on the parts that looked done: the first draft redirected seven other
`settle` mentions (snapshots, idempotency, dry-run, write latency, FR17, FR22) with a
single covering sentence, one of which sits *before* that sentence in document order —
every site now carries its own inline note, and FR22's determinism clause got the
opposite treatment it needed (it governs re-running the settlement *calculation* on the
read side, not a write verb). `docs/cli-json-contract.md` is named the operational source
of truth for the live surface; no contract change, so R31 did not fire. The sub-loop
subtracted two debts: #57 closed via PR #249 (quickpickle ≥1.11.2 makes `pixelmatch` an
optional lazy peer — the story-3.1 workaround devDep is gone, and with it the Dependabot
noise maint-28 had to probe-exclude) and the h11-pending `loop.csv` regen. Filed #254:
the usage-reader's Cost attribution claims a cwd filter it does not implement, so
parallel worktree sessions contaminate every Cost section — it reported $31.39 for this
docs-only story, and it blocks #176's push to make Cost mandatory. Next: unchanged —
Epic 5 · restore #232 · Module 6 (#100) per docs/status.md.
