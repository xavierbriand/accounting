---
date: 2026-08-10
story: maint-34
pr: 263
epic: maintenance
---

CLAUDE.md's Conflict-resolution protocol (§ 6.4.1) moved out of always-loaded context into an
on-demand `.claude/commands/rebase-conflict-protocol.md` command — a `/doctor` health-check
proposal the user approved, then formalized through the standard plan/review/retro shape.
Registered in `docs/harness/control-inventory.md` (R27/drift-scan Check F) in the same commit as
the file it describes. Opened as `story-maint-32`; a Phase-4 `sibling-overlap` re-check caught a
live collision — a concurrent session had independently renumbered an unrelated PR (#247, itself a
renumbering of an earlier `maint-30` collision) to that same id and merged it mid-flight, and the
next candidate (`maint-33`) was already claimed by yet another concurrent PR — so this story
renumbered to `maint-34`, rewriting its branch history under the corrected id before merging.
Filed two follow-ups from the Phase-4 findings: #265 (dod-check's prep-commit-subject regex
doesn't match CLAUDE.md's own canonical phrasing — a pre-existing gap shared by three earlier
stories) and #266 (R23's story-id uniqueness check is only point-in-time and doesn't hold up under
this repo's current level of concurrent-session activity). Next: unchanged — Epic 5 · restore
#232 · Module 6 (#100) per docs/status.md.
