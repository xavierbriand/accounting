---
date: 2026-08-11
story: maint-38
pr: 276
---

Ratcheted CI coverage thresholds for `src/infra` (78% branches) and the in-process-tested slice
of `src/cli` (`src/cli/commands/**` 84%, `src/cli/utils/**` 73%), each set ~5 points below a
measured 2026-08-11 baseline, resolving #242. The composition-root CLI files (`program.ts`,
`migrate.ts`, `ledger-command.ts`) stay outside the gate — they're validated by the mandatory
subprocess test instead, since v8 instrumentation can't observe a subprocess. Phase 4 review
caught a factual error in the plan's own claim about vitest's threshold-check behavior on a
misconfigured glob; filed as a follow-up ([#277](https://github.com/xavierbriand/accounting/issues/277))
rather than fixed in-story, since the gap is pre-existing (also affects the `src/core/**`
threshold from story-maint-29). Also filed [#278](https://github.com/xavierbriand/accounting/issues/278)
for a 4th-occurrence CLAUDE.md § 6 lane-trigger wording gap around root-level config files.
