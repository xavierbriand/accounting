---
date: 2026-08-11
story: maint-39
pr: 279
---

Added an integration test guarding every `coverage.thresholds` glob key against silently
matching zero files (the `istanbul-lib-coverage` `percent(0,0) === 100` failure mode found during
story-maint-38's Phase-4 review, tracked as #277). Extracted the thresholds object into
`vitest.coverage-thresholds.ts` so the test validates the live config rather than a hand-copied
key list — a future rename or typo is caught automatically. Also closed
[#242](https://github.com/xavierbriand/accounting/issues/242), which story-maint-38 had actually
delivered but never closed (its PR title referenced the issue without a closing keyword) — caught
by this story's own Phase-4 review.
