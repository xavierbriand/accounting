// STUB — implementation lands in slice 7.
// enumerateMonthStarts and dayBefore are helpers for buffer-topup line items.

export function enumerateMonthStarts(_from: string, _to: string): readonly string[] {
  return [];
}

export function dayBefore(_date: string): string {
  return '';
}
